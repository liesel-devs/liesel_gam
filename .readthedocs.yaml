# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

# Set the OS, Python version, and other tools you might need
build:
  os: ubuntu-24.04
  apt_packages:
    - graphviz
  tools:
    python: "3.13"
  jobs:
    install:
      - python -m pip install --upgrade pip
      - python -m pip install . --group dev
      - python -m pip install "liesel @ git+https://github.com/liesel-devs/liesel.git@de73db78af7f9385d1b50db01aa90a25e882e634"
      - python -m pip list
    build:
      html:
        - python -m sphinx -T -b html -j 3 -W --keep-going -d _build/doctrees docs/source "$READTHEDOCS_OUTPUT/html"

# Build documentation in the "docs/" directory with Sphinx
sphinx:
   configuration: docs/source/conf.py

# Optionally, but recommended,
# declare the Python requirements required to build your documentation
# See https://docs.readthedocs.io/en/stable/guides/reproducible-builds.html
# python:
#    install:
#    - requirements: docs/requirements.txt
